# Smithery.ai configuration https://smithery.ai/docs/config#smitheryyaml
startCommand:
  type: stdio
  configSchema:
    {}
  commandFunction:
    |-
    (config) => ({
      "command": "wecom-bot-mcp-server",
      "args": []
    })
